parameters:
    git_dir: .
    bin_dir: ./vendor/bin
    tasks:
      git_blacklist:
        keywords:
          - "die("
          - "var_dump("
          - "exit"
          - "##DEBUG##"
      phpunit:
